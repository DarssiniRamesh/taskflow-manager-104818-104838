/**
 * API service for backend interaction.
 * Fill in with actual HTTP methods and API endpoints.
 */

const API_ROOT = process.env.REACT_APP_API_ROOT || 'http://localhost:3001';

// Example function placeholder
// PUBLIC_INTERFACE
export async function loginUser(email, password) {
  // return fetch(`${API_ROOT}/auth/login`, ...)
  return { success: true };
}
